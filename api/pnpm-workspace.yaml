# Standalone project (not part of the root pnpm workspace).
# pnpm 11 errors on un-acknowledged dependency build scripts. Tailwind's native
# watcher and esbuild's Rails JavaScript build both need their platform hooks.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
