# Standalone project (not part of the root pnpm workspace).
# pnpm 11 errors on un-acknowledged dependency build scripts. esbuild installs
# its platform binary via a postinstall, needed for `pnpm build` (the Rails
# jsbundling step), so its build is explicitly allowed.
allowBuilds:
  esbuild: true
