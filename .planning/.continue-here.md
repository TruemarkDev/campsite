---
context: default
phase: campsite-d5w
task: 3
total_tasks: 12
status: in_progress
last_updated: 2026-08-11T19:42:52Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> The Kamal/Docker build blockers are fixed, validated locally, and committed at `79660251a015700c93e47f5bfa96790782675337`. One old-revision image remains in the operator-local registry. Nothing from the repaired revision is tagged, pushed, deployed, or accepted.

- [x] EXACT REVISION — The original packaging implementation is committed at `f77ad81f0e5d`; the build repair is committed at `79660251a015700c93e47f5bfa96790782675337`. Use the latter exact SHA for every repaired image; never retag it as `f77ad81`.
- [x] PNPM 11 FROZEN INSTALL — `pnpm-lock.yaml` now carries pnpm 11's canonical patch hash; frozen installs pass in the root, API, and HTML-to-image workspaces.
- [x] TRUSTED PNPM AUTH — Registry routing is non-secret and the ignored encrypted mise/user-auth path resolves the private Tiptap package without a project `.npmrc`.
- [x] SHADOW WORKER SAFETY — Rails web and Sidekiq are separate Kamal services. The API web config cannot consume queues; the worker remains explicitly withheld pending writer custody.
- [ ] SECRET CUSTODY — An ignored environment-only Kamal bridge and name-only preflight exist, but required runtime/deploy values are still missing. Never print or add them to Git.
- [ ] DEPENDENCY SECURITY — P0 `campsite-d5w.14` blocks shadow deployment until image-scoped critical/high advisories are resolved or explicitly mitigated.
- [ ] HISTORICAL CREDENTIALS — P0 `campsite-d5w.15` blocks provider transition until all 36 redacted history findings are dispositioned and possibly live credentials are rotated.
- [ ] SHURI SEARCH BOUNDARY — Do not boot production Elasticsearch until authentication and an Odin-only Shuri firewall rule are applied and verified atomically.

## Critical Anti-Patterns

| Pattern                                            | Severity | Prevention                                                                                                                                        |
| -------------------------------------------------- | -------: | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dirty-tree revision tagging                        | blocking | Commit a reviewed coherent scope, recheck index/HEAD, then build that exact revision.                                                             |
| Secret or provider side effects during image build | blocking | Keep BuildKit limited to the private package read token; source-map publication is a separate authorized release action.                          |
| Scheduler-disabled means worker-disabled           | blocking | Deploy Sidekiq only from the separate worker config after explicit writer custody.                                                                |
| Repo-wide audit count equals image exposure        | blocking | Produce SBOM-scoped reports for each deployable image under `campsite-d5w.14`.                                                                    |
| Health equals deployment acceptance                | advisory | Verify immutable digest/revision, roles, external host-header routing, authenticated browser flows, and negative side-effect evidence separately. |

<current_state>

Bead `campsite-d5w.3` remains in progress. The original packaging scope is committed at `f77ad81f0e5d`; the Kamal secret isolation, Node/Turbo/re2 build repair, and refreshed lockfile are committed at `79660251a015700c93e47f5bfa96790782675337`. The old-revision HTML-to-image image is still present only in the operator Mac's loopback registry. No repaired image has been pushed and nothing is deployed.

There is no GSD phase directory, so this remains the default `.planning` continuation record. Beads is authoritative.

Live read-only snapshot refreshed during this continuation:

- Operator Mac: arm64 Docker client/server.
- Odin `debian@192.168.2.41`: reachable, x86_64/amd64, Docker 29.6.2, approximately 76 GiB free on `/`; both Kamal remote BuildKit builders are running.
- Shuri `shuri@192.168.2.14`: reachable, arm64, Docker 29.5.2.
- Kamal 2.12.0: all seven Campsite configs render through the dedicated tracked, value-free `.kamal/campsite-secrets` bridge. Legacy Doppler-backed `.kamal/secrets-common` and per-service secret files are byte-for-byte untouched.
- Kamal's `localhost:5555` registry is the operator Mac's loopback `kamal-docker-registry`, SSH-forwarded to Odin during builds—not a registry listener on Odin. It is running `registry:3` at digest `sha256:1be55279f18a2fe1a74edf2664cac61c1bea305b7b4642dab412e7affdcb3e33` with persistent anonymous-volume storage. `truemarkdev/campsite-html-to-image` now exists; the other four target repositories remain absent.
- No background build, deployment, watcher, or async job is running. The disposable detached `f77ad81` build worktree was removed after the exact-revision attempt stopped.

</current_state>

<completed_work>

Completed Beads:

- `campsite-d5w.1`: topology/data/capacity audit.
- `campsite-d5w.2`: declared homelab production design.

Advanced `campsite-d5w.3` during this continuation:

- Regenerated the root lock with pnpm 11.9.0; root, API, and HTML-to-image frozen installs pass.
- Added ignored environment-only Kamal secret bridging and executable `script/check-campsite-kamal-secrets`; build-scope preflight passes for all seven configs without printing values.
- Split Sidekiq into `config/deploy.campsite-worker.yml`; the API config now runs web only.
- Pinned Dockerfile frontend, Ruby, Node, and Elasticsearch images by digest; removed API's dynamic Volta installer; all five application runtimes use unprivileged users.
- Removed Sentry/Glitchtip source-map upload and telemetry from image builds so builds do not mutate providers.
- Passed Docker build static checks for web, API, sync, styled-text, and HTML-to-image.
- Passed config package tests, Rails configuration tests (2 runs/12 assertions), sync build, styled-text tests (23/23), styled-text lint, HTML-to-image lint, and `git diff --check`.
- Diagnosed `document is not defined` during Next page collection as two independent Lottie SSR import paths. Both regressions now have Node-environment tests, and a telemetry-disabled Next 14.2.35 production build completes all 42 static pages, including `/[org]/chat/new`.
- Current migration diff is Gitleaks-clean. Full history has 36 redacted pre-existing findings tracked by P0 `campsite-d5w.15`; values were not inspected or printed.
- Narrow dependency upgrades reduced the root production audit from 4 critical/74 high to 2 critical/70 high. Remaining image-scoped remediation is tracked by P0 `campsite-d5w.14` and blocks `campsite-d5w.7`.
- Updated `docs/deployment/homelab-production.md` for separate worker custody, secret preflight, digest pinning, non-root runtimes, and provider-safe builds.
- Human-confirmed and archived the Lottie debug session under `.planning/debug/resolved/`.
- Created the narrow local exact-revision commit `f77ad81f0e5d` (`feat(deploy): package Campsite for homelab`), excluding Beads and `.planning` state. Root/API Docker contexts now exclude ignored secrets, auth, tracker, and planning files.
- Built and pushed `truemarkdev/campsite-html-to-image:f77ad81f0e5db297adfd9ac2906d335de8fb7824` serially on Odin. Its exact-version and `latest` tags both resolve to manifest-list digest `sha256:c60faf9de8558b031bcfd76c08f03950f6a09562567edeeb46e1d1632ff4edca`; the application manifest is `linux/amd64` at `sha256:3591089bdc019cf30bf65a58de984a6991e75b5820660a2fe9899f9a7957561e`.
- Stopped before Docker execution for styled-text because Kamal attempted to fetch `KAMAL_REGISTRY_PASSWORD` and `KAMAL_REGISTRY_USER_NAME` from missing Doppler project `campsite-common`. No styled-text repository/tag was created, and sync/API/web were not attempted.
- No deployment, application pull, accessory, firewall, DNS, provider, database, object-store, or data mutation was performed.
- Added an explicit `secrets_path` to all seven Campsite Kamal configs and strengthened `script/check-campsite-kamal-secrets` to enforce the value-free environment bridge. All build-scope checks and config renders pass without reading the obsolete `campsite-common` Doppler project.
- Replaced the three broken pnpm global Turbo installs with a pinned `pnpm dlx turbo@2.10.9 prune` flow that parses the pnpm 11 lockfile.
- Upgraded the transitive native binding to requested `re2@1.26.1`, pinned Node 22.22.2 by official multi-platform digest across all five runtime Dockerfiles, and added the web builder toolchain/build-script policy required by pnpm 11.
- Completed cache-only, no-push image builds for styled-text and sync during diagnosis, then completed the repaired web image end to end on Node 22.22.2: `re2@1.26.1` artifact install, production compile, lint/type checks, 42/42 static pages, and final runner assembly.
- Revalidated all seven Kamal build-scope secret checks/config renders, all five Dockerfile static checks, frozen lockfile state, legacy secret-file immutability, value-free bridge content, and `git diff --check`.
- Created the authorized wide local repair commit `79660251a015700c93e47f5bfa96790782675337` (`fix(deploy): restore reproducible Campsite image builds`) containing the coherent 17-file build repair. Beads and `.planning` state remain outside the product commit. No Git push, image push, or deploy was performed.

</completed_work>

<remaining_work>

1. Build all five images serially at commit `79660251a015700c93e47f5bfa96790782675337` (HTML-to-image, styled-text, sync, API, web), stopping on the first failure; record immutable tag/digest and prove amd64 after each success. Do not reuse the old `f77ad81` tag for repaired source.
2. Generate per-image SBOM/advisory evidence and resolve P0 dependency-security Bead `campsite-d5w.14` before shadow deployment.
3. Supply missing runtime secrets through encrypted/ignored custody and make `script/check-campsite-kamal-secrets` pass in deploy scope.
4. Complete `campsite-d5w.4` provisioning for MySQL, Redis, Elasticsearch, and object storage.
5. Complete backups and restore/data rehearsal (`d5w.5` and `d5w.6`).
6. Deploy a side-effect-disabled shadow stack (`d5w.7`) only after its dependency gates pass; verify exact revision, external authenticated flows, and negative side-effect evidence.
7. Complete historical credential/provider transition gate `campsite-d5w.15`, then proceed through provider rehearsal and cutover gates.

</remaining_work>

<decisions_made>

- Build custom images on the destination architecture: Mac for Shuri arm64, Odin for Debian/amd64. Forge is unused for Campsite.
- All current custom Campsite services—including HTML-to-image—run on Odin and therefore build on Odin. Shuri pulls the pinned upstream Elasticsearch multi-platform image.
- For a remote builder with `registry.server: localhost:5555`, Kamal 2.12.0 runs the registry on the operator Mac and SSH-forwards it to Odin. The build mutates Odin's BuildKit cache and adds version/`latest` tags to the Mac-local registry; it does not deploy an application container.
- Pnpm is the only supported JS workspace package manager; no project `.npmrc` or token-bearing Docker ARG/layer.
- BuildKit may receive only the private package read token. Sentry/Glitchtip release and source-map publication require a separately authorized release action.
- Rails web and Sidekiq have separate deploy configs. Exactly one worker custody may be active during migration.
- Keep the polo-apps stack authoritative until tokdio authenticated UAT, provider validation, restored backups, rollback rehearsal, and soak pass.

</decisions_made>

<blockers>

- **Remaining image builds:** the secret/Turbo/pnpm/re2 blockers are repaired and committed at `79660251a015700c93e47f5bfa96790782675337`. Exact-version pushes require explicit build-push authority for this SHA.
- **Secrets:** the bridge exists, but API/worker runtime values, web revalidation token, sync/styled Sentry DSNs, styled `AUTHTOKEN`, and other named deploy values are absent.
- **Dependency security:** `campsite-d5w.14` records 2 critical/70 high root production-audit findings, 7 high API JS findings, and 2 high HTML-to-image findings pending runtime/image classification and remediation.
- **Historical credentials:** `campsite-d5w.15` must disposition 36 redacted full-history findings before provider transition.
- **Stateful services:** MySQL, Redis, and object storage are not provisioned; production Elasticsearch firewall/auth is unproven.
- **Registry:** the operator Mac's loopback registry currently holds unrelated Recap/Tokdio/shadow repositories and has persistent anonymous-volume storage but no declared backup. Do not remove or recreate it as rollback; the five new repositories are absent and initial pushes are additive.
- **Real images:** only old-revision HTML-to-image is pushed/architecture-proven and it is not scanned. Styled-text, sync, and web have cache-only local build evidence from the repaired dirty tree; API has only static-check evidence. None of those four has a registry tag.

</blockers>

## Required Reading (in order)

1. `AGENTS.md` — repository safety, Beads, and conservative Git policy.
2. `bd show campsite-d5w.3`, `bd show campsite-d5w.14`, `bd show campsite-d5w.15`, and `bd show campsite-d5w.4` — authoritative task/dependency state.
3. `docs/deployment/homelab-production.md` — topology, architecture, capacity, recovery, and promotion gates.
4. `config/deploy.campsite-{web,api,worker,sync,styled-text,html-to-image,shadow}.yml` — exact services and targets.
5. `script/check-campsite-kamal-secrets` and tracked value-free `.kamal/campsite-secrets` — name-only custody/preflight; never print values.
6. `pnpm-workspace.yaml`, `pnpm-lock.yaml`, and `mise.toml` — registry, toolchain, overrides, and patch state.
7. `.planning/debug/resolved/web-build-lottie-ssr.md` — resolved two-path SSR root cause and human-confirmed build evidence.
8. `/Users/prakash/workspaces/2026/homelab/inventory/homelab.md` — host identity and architecture; still verify live before mutation.

## Critical Anti-Patterns (do NOT repeat these)

- Do not build a dirty checkout while tagging it as the old `HEAD`.
- Do not put Tiptap auth in a project `.npmrc`, Docker ARG, or image layer.
- Do not restore source-map upload/provider telemetry to image builds.
- Do not deploy the separate worker until explicit writer custody is granted.
- Do not use npm; use mise-managed pnpm 11.
- Do not build Odin/amd64 images locally on the ARM Mac; serialize builds on Odin.
- Do not boot production Elasticsearch before firewall/auth is in place.
- Do not claim deployment from config rendering, static checks, a completed build, or local health alone.

## Infrastructure State

- Odin and Shuri were reachable; only read-only architecture/Docker/disk probes were run.
- No Campsite container or accessory was started.
- No Cloudflare, DNS, provider, firewall, database, object-store, or host mutation was performed.
- Old `camp*.polo-apps.com` production remains authoritative and must remain untouched.
- `mise.local.toml` and `.kamal/secrets` are ignored. Do not copy their ciphertext/placeholders as if portable secret custody were proven.

## Pre-Execution Critique

The required engineering/infra/React critique is complete. It directly produced the pnpm reproducibility fix, separate worker custody, name-only secret gate, digest/non-root hardening, provider-safe build behavior, Lottie SSR regression coverage, and P0 security Beads. Full deployment remains blocked by the unresolved items above. Build-only work may resume after exact-revision authority; deployment additionally requires secrets, `d5w.14`, stateful custody, and approved host mutations.

<context>

The immediate objective remains reproducible, exact-revision images—not cutover. Build all five images serially on Odin at repair commit `79660251a015700c93e47f5bfa96790782675337`:

```bash
mise exec -- kamal build push --version=79660251a015700c93e47f5bfa96790782675337 -c config/deploy.campsite-html-to-image.yml
mise exec -- kamal build push --version=79660251a015700c93e47f5bfa96790782675337 -c config/deploy.campsite-styled-text.yml
mise exec -- kamal build push --version=79660251a015700c93e47f5bfa96790782675337 -c config/deploy.campsite-sync.yml
mise exec -- kamal build push --version=79660251a015700c93e47f5bfa96790782675337 -c config/deploy.campsite-api.yml
mise exec -- kamal build push --version=79660251a015700c93e47f5bfa96790782675337 -c config/deploy.campsite-web.yml
```

Before each build, render the config and run build-scope secret preflight by name only. After each build, record immutable tag/digest, prove amd64, generate an image-scoped SBOM/audit, and do not deploy. The worker is not part of the initial build sequence unless writer custody is explicitly authorized.

</context>

<next_action>

Request explicit authority for the five exact-version Kamal build pushes at `79660251a015700c93e47f5bfa96790782675337`. This does not authorize deployment, application pull, accessories, firewall, DNS, provider, or data mutations.

</next_action>
